#include "main.h"
/**
 * _isupper - check for uppercase
 * Return: 1 is upper or 0
 * @c: char to check
 */
*/
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
