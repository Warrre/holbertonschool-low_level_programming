#include "main.h"
#include <stdio.h>
/**
 * mul.c - 2-mul.c
 * @a: The first integer
 * @b: The second integer
 * Return: 1 if c is a digit, 0 otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
